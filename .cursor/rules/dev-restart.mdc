---
description: 在本项目中完成代码修改后重启开发服务
alwaysApply: true
---

# 开发服务重启

在本仓库（npm workspaces：`server` + `web`）中**完成与运行相关的修改后**（例如服务端、前端、`package.json`、环境配置等），应执行：

1. **若已有 `npm run dev` 在运行**：先停止该终端中的进程（或结束占用 3001 / 5173 的对应 Node 进程），再重新启动。
2. **在项目根目录**执行：`npm run dev`（同时启动后端与 Vite 前端）。

纯文档或未影响运行时的改动可不必重启。
